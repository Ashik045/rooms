/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['www.pexels.com', 'images.pexels.com', 'https://www.pexels.com']
  }
}

module.exports = nextConfig
